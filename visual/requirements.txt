# Visual layer only. It sits on top of the ghost, so install both:
#   pip install -r requirements.txt -r visual/requirements.txt

fastapi>=0.110.0            # HTTP + websocket server
uvicorn[standard]>=0.28.0   # ASGI server (bundles websockets, watchfiles)
